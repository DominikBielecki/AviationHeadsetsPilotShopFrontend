import { AviationPilotShopValidators } from './aviation-pilot-shop-validators';

describe('AviationPilotShopValidators', () => {
  it('should create an instance', () => {
    expect(new AviationPilotShopValidators()).toBeTruthy();
  });
});
